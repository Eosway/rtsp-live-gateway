{
  "name": "rtsp-live-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @rtsp-gateway/protocol build && npm run -w @rtsp-gateway/shared build && npm run --workspaces --if-present build",
    "typecheck": "npm run -w @rtsp-gateway/protocol build && npm run -w @rtsp-gateway/shared build && npm run --workspaces --if-present typecheck",
    "test": "npm run --workspaces --if-present test",
    "lint": "npm run --workspaces --if-present lint",
    "clean": "npm run --workspaces --if-present clean"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
