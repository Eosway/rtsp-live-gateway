{
  "name": "@eosway/rtsp-live-gateway-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "tsc": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@eosway/rtsp-live-gateway-protocol": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "catalog:"
  }
}
